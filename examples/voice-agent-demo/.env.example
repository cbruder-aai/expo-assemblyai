# URL of the backend that mints ephemeral AssemblyAI tokens (see token-server.js).
# Never put your AssemblyAI API key in the app — only this endpoint URL.
EXPO_PUBLIC_TOKEN_ENDPOINT=http://localhost:8787
